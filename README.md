# data_science_data_exploration
with pandas

needs to have "webdriver-manager" to run 

1. create venv by python3 -m venv venv in the folder you're working on
2. source venv/bin/activate
3. then pip install webdriver-manager
